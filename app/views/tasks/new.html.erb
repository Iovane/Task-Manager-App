<% if logged_in? %>
  <div class="container mt-5">
    <h1>New Task</h1>
  </div>
  <%= render 'form', obj:@task %>
<% else %>
  <% flash.now[:alert] = "You must be logged in to create task" %>
  <%= render 'pages/home' %>
<% end %>